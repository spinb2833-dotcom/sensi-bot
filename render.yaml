services:
  - type: web
    name: sensi-bot
    env: php
    buildCommand: ""
    startCommand: php -S 0.0.0.0:10000
    envVars:
      - key: DATABASE_URL
        fromDatabase:
          name: sensi-db
          property: connectionString

databases:
  - name: sensi-db
    plan: free
